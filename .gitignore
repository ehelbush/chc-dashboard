# Schwab credentials & tokens (NEVER commit these)
.env
schwab_tokens.json

# Python
__pycache__/
*.pyc
venv/

# OS
.DS_Store
node_modules/
logs/
*.bak

# Screener in-progress file (promoted to market_screener.json only when the run clears the coverage floor)
data/market_screener.partial.json
